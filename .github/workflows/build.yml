name: Build Home Assistant Add-ons

on:
  push:
    branches:
      - main
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Build Home Assistant add-ons
        uses: home-assistant/builder@2024.12.1
        with:
          args: |
            --all \
            --target /data
